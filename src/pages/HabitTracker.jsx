import React from 'react';
import HabitTrackerContainer from "../containers/HabitTrackerContainer.jsx";

const HabitTracker = () => {
    return (
        <section>
            <HabitTrackerContainer/>
        </section>
    );
};

export default HabitTracker;