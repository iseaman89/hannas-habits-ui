import AuthContainer from "../containers/AuthContainer.jsx";

const AuthPage = () => {
    return (
        <section>
            <AuthContainer/>
        </section>
    );
};

export default AuthPage;