import React from 'react';
import ResolutionContainer from "../containers/ResolutionContainer.jsx";

const Resolution = () => {
    return (
        <section>
            <ResolutionContainer/>
        </section>
    );
};

export default Resolution;